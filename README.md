# veil-shopify

Shopify theme repository, connected via Shopify's GitHub integration
(Online Store → Themes → Connect from GitHub).

Once connected, Shopify pushes the theme's code (`layout/`, `templates/`,
`sections/`, `snippets/`, `assets/`, `config/`, `locales/`) into this
branch, and syncs changes both ways.
